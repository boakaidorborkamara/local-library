<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <!-- bootstrap link  -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    
   <div class="container-fluid">
      <div class="row ">
        <!-- nav -->
        <%- include("./partial/nav") -%>
    
        <main class="col-lg-10"> 

           <!-- Dynamicly add page title  -->
          <%if(typeof title !== "undefined"){%>
                <div>
                  <h1><%= title %></h1>
                </div>
            <%} 
          %>


          <!-- Add list of author  -->
          <% if(typeof authors !== "undefined"){%>

              <ul>
                <!-- loop through array of author and create an unorder list  -->
                <%
                  authors.forEach((author)=>{%>
                      <li>
                        <a href=<%= author.id %>>
                          <%= author.firstName %> <%= author.FamilyName %>
                        </a>

                        <!-- Show date of birth and date of dead if exist  -->
                        (<strong>Birth Date:</strong> <%= author.date_of_birth %> - <strong>Death Date:</strong> <%= author.date_of_death %>)
                      </li>
                  <%})
                %>
              </ul>
             

            <%}
            else{%>
              <p>No Author</p>
            <%}
          %>
        
        </main>
      </div>
   </div>
  </body>
</html>
